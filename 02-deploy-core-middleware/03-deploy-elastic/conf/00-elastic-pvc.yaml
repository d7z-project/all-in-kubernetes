apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-elastic-data
  namespace: core-middle
  labels:
    app: elastic
spec:
  storageClassName: sc-nfs-share
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi

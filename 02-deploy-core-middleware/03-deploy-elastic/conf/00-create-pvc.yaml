apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-elastic-data
  namespace: core-middle
  labels:
    type: elastic
spec:
  storageClassName: cluster-share
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
